---
import { getPath, type Post, renderPost, slugifyStr } from "@/utils"
import PostInfo from "./PostInfo.astro"

export interface Props extends Post {
  variant?: "h2" | "h3"
}

const { variant = "h2", id, filePath } = Astro.props

const { title, description, publishDate, updateDate, readingTime } = await renderPost(Astro.props)

const transitionName = slugifyStr(title)
---

<li class="my-6">
  <a
    href={getPath(id, filePath)}
    class="inline-block text-lg font-medium text-accent decoration-dashed underline-offset-4 focus-visible:no-underline focus-visible:underline-offset-0"
  >
    {
      variant === "h2" ? (
        <h2 transition:name={transitionName} class="text-lg font-medium decoration-dashed hover:underline">
          {title}
        </h2>
      ) : (
        <h3 transition:name={transitionName} class="text-lg font-medium decoration-dashed hover:underline">
          {title}
        </h3>
      )
    }
  </a>
  <PostInfo {publishDate} {updateDate} {readingTime} />
  <p>{description}</p>
</li>
