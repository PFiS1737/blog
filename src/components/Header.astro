---
import { ArchiveIcon, SearchIcon, ThreeBarsIcon, XIcon } from "@primer/octicons-react"
import config from "@/site.config"
import Hr from "./Hr.astro"
import LinkButton from "./LinkButton.astro"

const { pathname } = Astro.url

const isActive = (target: string) => {
  const pathArray = pathname.split("/")
  const targetArray = target.split("/")

  return pathname === target || pathArray[1] === targetArray[1]
}
---

<header>
  <a
    class="absolute inset-s-16 -top-full z-50 bg-background px-3 py-2 text-accent backdrop-blur-lg transition-all focus:top-4"
    href="#main-content"
    id="skip-to-content"
  >
    Skip to content
  </a>
  <div class="mx-auto flex max-w-app flex-col items-center justify-between sm:flex-row" id="nav-container">
    <div
      class="relative flex w-full items-baseline justify-between bg-background p-4 sm:items-center sm:py-6"
      id="top-nav-wrap"
    >
      <a
        class="absolute py-1 text-xl leading-8 font-semibold whitespace-nowrap sm:static sm:my-auto sm:text-2xl sm:leading-none"
        href="/"
      >
        {config.title}
      </a>
      <nav
        class="flex w-full flex-col items-center sm:ms-2 sm:flex-row sm:justify-end sm:space-x-4 sm:py-0"
        id="nav-menu"
      >
        <button
          aria-controls="menu-items"
          aria-expanded="false"
          aria-label="Open Menu"
          class="self-end p-2 sm:hidden"
          id="menu-btn"
          type="button"
        >
          <ThreeBarsIcon />
          <XIcon className="hidden" />
        </button>
        <ul
          id="menu-items"
          class:list={[
            "hidden",
            "mt-4 flex flex-col place-content-center gap-2",
            "[&>li>a]:block [&>li>a]:py-3 [&>li>a]:text-center [&>li>a]:font-medium [&>li>a]:hover:text-accent sm:[&>li>a]:px-2 sm:[&>li>a]:py-1",
            "sm:flex sm:mt-0 sm:flex-row sm:gap-x-5 sm:gap-y-0",
          ]}
        >
          <li>
            <a href="/posts" class:list={{ "active-nav": isActive("/posts") }}> Posts </a>
          </li>
          <li>
            <a href="/tags" class:list={{ "active-nav": isActive("/tags") }}> Tags </a>
          </li>
          <li>
            <a href="/about" class:list={{ "active-nav": isActive("/about") }}> About </a>
          </li>
          <li>
            <LinkButton
              href="/archives"
              title="Archives"
              class:list={{
                "active-nav [&>svg]:text-accent [&>svg]:stroke-accent": isActive("/archives"),
              }}
            >
              <ArchiveIcon className="hidden sm:block" size={24} />
              <span class="sm:sr-only">Archives</span>
            </LinkButton>
          </li>
          <li>
            <LinkButton
              href="/search"
              title="Search"
              class:list={{
                "active-nav [&>svg]:text-accent [&>svg]:stroke-accent": isActive("/search"),
              }}
            >
              <SearchIcon className="hidden sm:block" size={24} />
              <span class="sm:sr-only">Search</span>
            </LinkButton>
          </li>
        </ul>
      </nav>
    </div>
  </div>
  <Hr />
</header>

<script>
  function toggleNav() {
    const menuBtn = document.querySelector("#menu-btn")
    const menuItems = document.querySelector("#menu-items")
    const menuIcon = menuBtn?.querySelector("svg:first-child")
    const closeIcon = menuBtn?.querySelector("svg:last-child")

    if (!(menuBtn && menuItems && menuIcon && closeIcon)) {
      return
    }

    menuBtn.addEventListener("click", () => {
      const openMenu = menuBtn.getAttribute("aria-expanded") === "true"

      menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true")
      menuBtn.setAttribute("aria-label", openMenu ? "Open Menu" : "Close Menu")

      menuItems.classList.toggle("hidden")
      menuIcon.classList.toggle("hidden")
      closeIcon.classList.toggle("hidden")
    })
  }

  toggleNav()

  // Runs on view transitions navigation
  document.addEventListener("astro:after-swap", toggleNav)
</script>
